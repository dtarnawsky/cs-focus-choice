import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.focus.choice',
  appName: 'cs-focus-choice',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
